# Nobody edits the same file. This makes that mechanical instead of
# remembered at 4am. See WIRE_PROTOCOL.md for the reasoning.

/graph/       @person-b
/contract/    @person-b
/scoring/     @person-a
/data/        @person-c
/demo/        @person-c

# fixtures/ is C's, except the one file B seeds at hour 1
/fixtures/                              @person-c
/fixtures/candidate_rings.sample.json   @person-b

# CI + this file itself
/.github/     @person-b

# artifacts/*.json is committed, not gitignored (the decoupling
# mechanism — see WIRE_PROTOCOL.md §2), so single-writer ownership here
# matters more than almost anywhere else in the repo.
/artifacts/candidate_rings.json   @person-b
/artifacts/scored_rings.json      @person-a
